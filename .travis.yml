branches:
  except:
  - "/^wip/"
  - "/^dependabot/"

language: python

python: 3.8
cache: pip

install:
- pip install flake8
- pip install -e .
- python3 -m spacy download en_core_web_sm

stages:
  - test

jobs:
  include:
    -
      name: "Main tests"
      stage: test
      script:
        - bash ./tests/run.sh
